name: Pipeline
on:
  push:
    branches:
      - devnet
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
      - name: Build 
        run: |
          cargo build --verbose
        working-directory: .
  test:
    needs: build
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
      - name: test 
        run: |
          cargo test --verbose
        working-directory: .
  deploy:
    name: Deploy
    uses: fenriz07/binding-workflow/.github/workflows/deploy.yml@master